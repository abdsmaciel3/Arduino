/* Como funciona a função MAP
 *  Sintaxe de definição da função map:
 *   long map(long x, long in_min, long in_max, long out_min, long out_max){
 *     return(x-in_min)*(out_max-out_out_min)/(in_max-in_min)+out_min;
 *     
 *     in--> faixa de entrada: valor min e max lidos no sensor
 *     out--> faixa de saida: valor min e max que serão o resultado retornado pela função map(valor de saída mapeado)
 */
void setup() {
  // put your setup code here, to run once:

}

void loop() {
  // put your main code here, to run repeatedly:

}
